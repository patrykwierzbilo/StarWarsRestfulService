@model IEnumerable<StartWarsRestfulService.Models.CharactersClass>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.episodes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.friends)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.planet)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.episodes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.friends)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.planet)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
